import React from 'react';
import "../../assets/scss/_sectionTitle.scss"

const SectionTitle = ({children}) => {
    return <div className="sectionTitle">{children}</div>;
};

export default SectionTitle;
